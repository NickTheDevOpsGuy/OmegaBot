services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DATABASE_PATH=/app/data/omegabot.db
      - METRICS_PORT=9090
    volumes:
      - omegabot_data:/app/data
    ports:
      - "${METRICS_PORT:-9090}:9090"

volumes:
  omegabot_data:
